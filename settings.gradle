include ':app'
rootProject.name = "VastUtils"

// =======
// = Lib =
// =======

def libs = [
        "VastTools",
        "VastAdapter",
        "VastActivity",
        "VastNetStateLayout",
        "VastSwipeView"
]

libs.forEach {
    include(":libraries:$it")
}
include ':libraries:VastAnnotation'
