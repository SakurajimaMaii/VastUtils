include ':app'
rootProject.name = "VastUtils"

// =======
// = Lib =
// =======

def libs = [
        "VastTools",
        "VastAdapter",
        "VastActivity",
        "VastNetStateLayout",
        "VastSwipeListView"
]

libs.forEach {
    include(":libraries:$it")
}