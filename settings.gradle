include ':app'
rootProject.name = "VastUtils"

// =======
// = Lib =
// =======

def libs = [
        "VastAdapter",
        "VastTools",
        "VastNetStateLayout",
        "VastSwipeRecyclerView",
]

libs.forEach {
    include(":libraries:$it")
}
