include ':app'
include ':appjava'
include ':appcompose'
rootProject.name = "VastUtils"

// =======
// = Lib =
// =======

def libs = [
        "VastAdapter",
        "VastTools",
        "VastNetStateLayout",
        "VastSkin",
        "VastSwipeRecyclerView",
        "VastUtils",
]

libs.forEach {
     include(":libraries:$it")
}

// =======
// = Skin =
// =======

def skin = [
        "darkskin"
]

skin.forEach {
     include(":skin:$it")
}