include ':app'
rootProject.name = "VastUtils"

// =======
// = Lib =
// =======

def libs = [
        "VastTools",
        "VastAdapter",
]

libs.forEach {
    include(":$it")
    project(":$it").projectDir = new File("libraries/$it")
}