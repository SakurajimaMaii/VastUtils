include ':app'
rootProject.name = "VastUtils"

// =======
// = Lib =
// =======

def libs = [
        "VastTools",
        "VastAdapter",
        "VastNetStateLayout",
        "VastSwipeRecyclerView",
]

libs.forEach {
    include(":libraries:$it")
}

include ':libraries:VastAnnotation'
