include ':app'
rootProject.name = "VastUtils"

// =======
// = Lib =
// =======

def libs = [
        "VastTools",
        "VastAdapter",
        "VastActivity",
        "VastNetStateLayout"
]

libs.forEach {
    include(":libraries:$it")
}